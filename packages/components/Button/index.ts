import Button from './Button.vue'
import ButtonGroup from './ButtonGroup.vue'
import { withInstall } from '@viz-ui-y/utils'

export const VizButton = withInstall(Button)
export const VizButtonGroup = withInstall(ButtonGroup)
export * from './types'
