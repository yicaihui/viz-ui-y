import Collapse from './Collapse.vue'
import CollapseItem from './CollapseItem.vue'
import { withInstall } from '@viz-ui-y/utils'

export const VizCollapse = withInstall(Collapse)
export const VizCollapseItem = withInstall(CollapseItem)

export * from './types'
